package state

// Pinned controls whether the sidebar stays visible regardless of mouse position.
var Pinned bool

// PanelY is the vertical position of the sidebar panel on screen (pixels from top).
var PanelY = 40
